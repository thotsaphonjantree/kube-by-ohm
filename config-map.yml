kind: ConfigMap
apiVersion: v1
metadata:
  name: nginx-config
  namespace: copter-goodguy
data:
  default.conf: |
    server {
      listen 80;
      server_name _;

      location / {
        return 200 "copter-goodguy is here!"
      }
    }
